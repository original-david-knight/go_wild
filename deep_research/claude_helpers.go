package deepresearch

const claudeJSONSuffix = `

IMPORTANT: Respond with ONLY the raw JSON object. Do not wrap the JSON in markdown code fences (no ` + "```" + `json or ` + "```" + `). Do not include any text before or after the JSON.`
